# Use cases

1. A developer runs `create-spec-project <target>`, declines the default OpenSpec prompt, and receives a new project skeleton.
2. A developer passes a destination path, selects Yes in the single OpenSpec prompt, and receives a scaffold followed by OpenSpec initialization.
3. A team fills in the generated specification documents before implementation.
