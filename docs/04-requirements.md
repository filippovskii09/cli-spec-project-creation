# Requirements

- Create the documentation, source, template, and test directories.
- Copy documentation and agent instruction templates into the new project.
- Prompt for project name, destination, and OpenSpec setup when values are not supplied.
- Refuse to overwrite an existing non-empty destination.

