# Requirements

- Create the documentation, source, template, and test directories.
- Copy documentation and agent instruction templates into the new project.
- Accept exactly one positional target path.
- Refuse a non-empty destination before showing the single OpenSpec confirmation prompt.
- Default the OpenSpec confirmation to No; scaffold first and run OpenSpec only after a Yes answer.
