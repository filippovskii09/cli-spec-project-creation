# Create Spec Project

This repository contains a TypeScript CLI that creates a documentation-first project skeleton.

- Keep templates in `templates/` as the canonical source for generated files.
- Keep filesystem side effects in `src/scaffold.ts`.
- Run `npm test` before handing off a change.

